enum TextSizes { small, medium, large }

enum ConnectionType { Mobile, Wifi }

enum ImageSourceType { Camera, Gallery }

enum OtpGetRequestStatus { error, success }

enum AddressType { permanentHome, currentHome, office, other }

enum PaymentMethods {
  paypal,
  googlePay,
  applePay,
  visa,
  masterCard,
  creditCard,
  payStack,
  razorPay,
  paytm,
  phonePay
}
